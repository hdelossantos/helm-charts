apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "yacy.fullname" . }}-test-connection"
  labels:
    {{- include "yacy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "yacy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
